a macOS menu-bar app that displays internet connectivity
written in python with uv as the package manager
uses the AnyBar dektop app to display the icon
project configuration and dependencies in @pyproject.toml
main source file @src/main.py
for more information read @README.md
